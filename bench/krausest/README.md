# krausest truth harness

Paired vanilla+spark runs of the official webdriver-ts harness against this
repo's core. `./run.sh --count 6` (headless dev loop), `--windowed --count 15`
for ledger numbers. `impl/` = the PR #2048 implementation. See
`spark-speed-up-max.md` §4-F0 and §5 (bench discipline).
