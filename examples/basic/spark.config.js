// Everything defaults: `spark dev` serves index.html + the component fragments
// in components/, with scoped hot-reload. No pipeline — this example is a
// dev-time tour of the runtime, not a deployable build.
export default {};
