name: Publish spark-prerender to npm

on:
  push:
    tags:
      - 'prerender-v*' # e.g. git tag prerender-v0.1.0 && git push --tags
  workflow_dispatch:

permissions:
  contents: read
  id-token: write # npm provenance

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: bun install

      - name: Run tests
        run: bun run test

      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/prerender-v')
        run: |
          PKG_VERSION=$(node -p "require('./packages/spark-prerender/package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#prerender-v}"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "Tag prerender-v$TAG_VERSION does not match package version $PKG_VERSION" >&2
            exit 1
          fi

      - name: Publish
        run: npm publish --provenance --access public
        working-directory: packages/spark-prerender
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}